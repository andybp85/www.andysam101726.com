{
    "private": true,
    "type": "module",
    "scripts": {
        "test": "node --test 'tests/**/*.test.js'",
        "pretest:browser": "npm link @playwright/test",
        "test:browser": "playwright test"
    }
}
